# Automotive Repair Management System - Environment Variables
# Copy this file to .env and update values
# NEVER commit .env to version control!

# =============================================================================
# FLASK APPLICATION
# =============================================================================

FLASK_ENV=development
SECRET_KEY=your-secret-key-generate-with-python-c-import-secrets-print-secrets-token-hex-32

# Server binding (local development)
HOST=127.0.0.1
PORT=5000

# =============================================================================
# DATABASE - Neon PostgreSQL
# =============================================================================

# Use DATABASE_URL for Neon (recommended)
# Get this from: neonctl connection-string PROJECT_ID
# Or from Neon Console > Connection Details
DATABASE_URL=postgresql://user:password@ep-xxx-xxx-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# Alternative: Individual variables (if not using DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=your-password
# DB_NAME=spb

# SSL mode: require (cloud), prefer (local), disable (testing)
DB_SSLMODE=require

# =============================================================================
# NEON AUTH - JWT Authentication (powered by Better Auth)
# =============================================================================

# Neon Auth URL - Get from Neon Console > Auth > Project Info
# Format: https://ep-xxx.neonauth.xxx.aws.neon.tech/dbname/auth
NEON_AUTH_URL=https://ep-xxx.neonauth.xxx.aws.neon.tech/neondb/auth

# JWKS URL for JWT token verification
# Format: {NEON_AUTH_URL}/.well-known/jwks.json
NEON_AUTH_JWKS_URL=https://ep-xxx.neonauth.xxx.aws.neon.tech/neondb/auth/.well-known/jwks.json

# =============================================================================
# LOGGING
# =============================================================================

LOG_LEVEL=INFO

# Set to true for Heroku/cloud deployments
LOG_TO_STDOUT=false

# =============================================================================
# HEROKU DEPLOYMENT
# =============================================================================

# These are automatically set by Heroku:
# - PORT (assigned by Heroku)

# You must set these manually:
# heroku config:set SECRET_KEY=$(python -c "import secrets; print(secrets.token_hex(32))")
# heroku config:set FLASK_ENV=production
# heroku config:set LOG_TO_STDOUT=true
# heroku config:set DATABASE_URL="postgresql://..."
# heroku config:set NEON_AUTH_URL="https://ep-xxx.neonauth.xxx/neondb/auth"
# heroku config:set NEON_AUTH_JWKS_URL="https://ep-xxx.neonauth.xxx/neondb/auth/.well-known/jwks.json"
